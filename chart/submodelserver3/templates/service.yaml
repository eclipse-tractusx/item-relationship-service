apiVersion: v1
kind: Service
metadata:
  name: {{ include "submodelserver3.fullname" . }}
  labels:
    {{- include "submodelserver3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: default
      protocol: TCP
      name: http
  selector:
    {{- include "submodelserver3.selectorLabels" . | nindent 4 }}
