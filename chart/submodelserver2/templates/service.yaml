apiVersion: v1
kind: Service
metadata:
  name: {{ include "submodelserver2.fullname" . }}
  labels:
    {{- include "submodelserver2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: default
      protocol: TCP
      name: http
  selector:
    {{- include "submodelserver2.selectorLabels" . | nindent 4 }}
