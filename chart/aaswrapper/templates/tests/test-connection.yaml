apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aaswrapper.fullname" . }}-test-connection"
  labels:
    {{- include "aaswrapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aaswrapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
