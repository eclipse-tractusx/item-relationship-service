apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "submodelserver4.fullname" . }}-test-connection"
  labels:
    {{- include "submodelserver4.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "submodelserver4.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
