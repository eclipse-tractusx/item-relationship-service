apiVersion: v1
kind: Service
metadata:
  name: {{ include "submodelserver4.fullname" . }}
  labels:
    {{- include "submodelserver4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: default
      protocol: TCP
      name: http
  selector:
    {{- include "submodelserver4.selectorLabels" . | nindent 4 }}
