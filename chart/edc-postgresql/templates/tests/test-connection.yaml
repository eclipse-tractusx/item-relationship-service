apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edc-esr-postgresql.fullname" . }}-test-connection"
  labels:
    {{- include "edc-esr-postgresql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edc-postgresql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
