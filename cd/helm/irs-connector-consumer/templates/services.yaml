apiVersion: v1
kind: Service
metadata:
  name: irs-connector-consumer
  labels:
    app.kubernetes.io/name: irs-connector-consumer
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8181
      protocol: TCP
  selector:
    app.kubernetes.io/name: irs-connector-consumer
