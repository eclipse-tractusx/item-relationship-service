server:
  port: 8081

blobstore:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin

irs:
  job:
    cleanup:
      scheduler:
        completed: 0 * * * * *
        failed: 0 * * * * *

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://catenaxintakssrv.germanywestcentral.cloudapp.azure.com/iamcentralidp/auth/realms/CX-Central/protocol/openid-connect/certs
      client:
        registration:
          keycloak:
            authorization-grant-type: client_credentials
            client-id: sa-cl6-cx-2
            client-secret: 15NnEltJeBZmvnlLmeH7SzNsBX9A4mVp
  config:
    activate:
      on-profile: local

connection:
  base-uri: http://localhost:8081
  access-token: https://catenaxintakssrv.germanywestcentral.cloudapp.azure.com/iamcentralidp/auth/realms/CX-Central/protocol/openid-connect/token